package graphics;

import javax.swing.*;
import java.awt.*;

public class GameFrame extends JFrame {
    public GameFrame(){
        setTitle("Catan");
        setSize(1920, 1080);
        setBackground(Color.BLACK);
    }
}
